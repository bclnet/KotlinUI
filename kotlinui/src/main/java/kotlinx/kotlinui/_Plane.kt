package kotlinx.kotlinui

import android.graphics.ColorSpace
import android.graphics.Color as CGColor

object _Plane {
    var colorSpaces = Array<ColorSpace?>(1) { null }
    var colors = Array<CGColor?>(14) { null }
}
